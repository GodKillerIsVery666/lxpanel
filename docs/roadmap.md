# 路线图

## v0.1

- 登录、初始化、会话与审计。
- 系统概览、进程、服务、文件目录。
- 连接器登记和心跳协议。
- 前端基础运维工作台。

## v0.2

- Docker 状态、容器列表、镜像列表和容器动作。
- 日志根目录浏览与尾部读取。
- 多用户、RBAC、受控任务运行器和本地状态备份。
- 计划任务和自动状态备份。
- 连接器命令队列和结果回传协议。
- 可选 SQLite 状态存储与 legacy JSON 导入。
- CPU、内存、磁盘资源告警。
- 主机资产、监控趋势和 Webhook 通知渠道。
- 内置应用商店和 Docker Compose 一键部署基础。
- API Token 自动化访问。
- API Token 细粒度作用域。
- Webhook 出站白名单与通知 URL 脱敏。
- Webhook URL 加密保存与旧状态兼容读取。
- 审计筛选、导出和保留清理。
- 高风险操作审批中心，覆盖备份恢复和审计清理。
- 备份完整性校验和恢复状态保真。

## 商业化迭代计划表

| 状态 | 能力 | 商业价值 | 备注 |
| --- | --- | --- | --- |
| ✅ | API Token 自动化访问 | 支持 CI、脚本和第三方系统接入 | Token 明文只展示一次，服务端存哈希 |
| ✅ | API Token 细粒度作用域 | 降低自动化密钥泄露后的 blast radius | 已按路由读写 scope 拦截 |
| ✅ | Webhook 出站白名单与 URL 脱敏 | 防止误投递到非预期目标，避免界面泄露密钥 | 已支持通配域名 |
| ✅ | Webhook URL 加密保存 | 防止状态文件直接暴露通知密钥 | 已兼容旧明文状态 |
| ✅ | 审计筛选、导出和保留清理 | 满足合规检索与留存管理 | 清理需审批 |
| ✅ | 高风险操作审批中心 | 让备份恢复、审计清理等动作可控可追溯 | 审批单一次性消费 |
| ✅ | 备份完整性校验和恢复状态保真 | 降低恢复前数据损坏和状态丢失风险 | 校验大小、SHA-256、格式和状态字段 |
| ✅ | API Token 到期提醒和过期态展示 | 提前发现即将失效的自动化密钥 | 安全页和安全巡检均已提示 |
| ✅ | 通知渠道密钥轮换与迁移工具 | 支持生产更换 `LXPANEL_SESSION_SECRET` | 已支持旧明文迁移和旧密钥重加密 |
| ✅ | 审批通知和多级审批 | 缩短高风险操作等待时间，适配团队流程 | 已接入系统事件通知，支持批准人数阈值和审批记录 |
| ✅ | 应用商店模板版本升级和回滚 | 提升应用生命周期管理能力 | 部署记录已版本化，支持变量重渲染和上一版本回滚 |
| ✅ | 远程备份 | 把关键状态复制到外部存储 | 已支持文件系统目标同步和 SHA-256 旁路校验文件 |
| ✅ | 数据库管理 | 覆盖常见面板高频需求 | 已支持 PostgreSQL 连接登记、URL 加密、手动 pg_dump 备份 |
| ✅ | 防火墙/SSH 加固 | 增强服务器安全治理闭环 | 安全页已输出防火墙、SSH、Docker 权限边界加固计划 |
| ✅ | E2E 测试、迁移脚本和安装升级体验 | 降低商业交付与升级风险 | 已提供 E2E smoke、状态迁移脚本和发布包脚本纳入 |
| ✅ | 对象存储远程备份 | 支持 S3、MinIO、COS、OSS 等外部存储，降低单机故障风险 | 已支持 S3 兼容目标、访问密钥加密、同步 SHA-256 校验对象 |
| ✅ | 数据库恢复演练和多引擎支持 | 从“能备份”升级为“可验证恢复”，覆盖更多客户环境 | 已支持 PostgreSQL、MySQL、MariaDB、保留天数和恢复演练记录 |
| ✅ | 多主机纳管和远程 SSH 会话 | 让面板从单机工具升级为小型集群运维入口 | 已支持主机组、标签目标、连接器批量命令和 SSH 会话命令排队 |
| ✅ | 资源级 RBAC 和工作空间隔离 | 支持团队、客户、项目维度授权，减少误操作和越权风险 | 已提供访问策略、资源匹配和平台治理页评估入口 |
| ✅ | 审计防篡改和合规报表 | 提升商业客户审计可信度，便于安全复盘和交付验收 | 新审计事件写入哈希链，提供完整性校验和合规统计接口 |
| ✅ | 应用市场签名和健康检查 | 降低第三方模板供应链风险，提升应用上线成功率 | 模板包含来源、签名、验证状态和部署健康探测 |
| ✅ | 自动化安全修复 | 把安全建议升级为可控修复闭环 | 平台治理页支持 dry-run 修复计划、执行记录和审批动作类型 |
| ✅ | 监控告警增强 | 提升生产可观测性，减少故障发现时间 | 已增加 Prometheus exporter、告警静默和平台化容量建议 |
| ✅ | 性能和容量优化 | 支持更长时间运行、更大状态数据和更多主机 | 已提供容量计划、状态增长评估和调度/归档优化建议 |
| ✅ | 图形化升级向导 | 降低客户现场升级风险和人工操作成本 | 平台治理页提供备份、迁移预检、兼容性和回滚步骤 |
| ✅ | 安装包和离线交付增强 | 提升商业交付可复制性，适配内网环境 | 已提供离线交付清单、安装自检项和发布包校验流程 |
| ✅ | API 和 Webhook 开放平台 | 支持生态集成与二次开发 | 已输出 OpenAPI 摘要、Webhook 事件目录和自动化入口说明 |

## 下一轮商业化计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | Web 终端交互代理 | 把 SSH 会话从命令排队升级为可交互终端 | 已提供终端会话、输入、关闭 API，基于连接器命令队列代理 |
| ✅ | 增加 | 应用模板仓库导入 | 支持企业私有模板源、版本索引和签名公钥管理 | 已支持模板仓库登记、签名信任模式和同步记录 |
| ✅ | 增加 | 多节点监控采集 | 从本机采样扩展到连接器主机资源采样 | 连接器心跳可上报 hostId 指标，监控页可切换远端主机 |
| ✅ | 增加 | 许可证与租户配额 | 支持商业授权、功能开关和主机/用户/应用数量限制 | 已提供离线许可证、用量统计和超额提示 |
| ✅ | 改进 | 资源级审批策略 | 将审批单绑定到主机、备份目标、数据库和应用资源 | 已提供资源审批策略登记，并与资源类型和动作关联 |
| ✅ | 改进 | 审计导出签名包 | 导出审计日志、完整性报告和签名 manifest | 已提供审计分页和 JSON 签名包 manifest SHA-256 |
| ✅ | 改进 | 数据库计划备份策略 | 支持按连接配置定时备份、保留清理和失败通知 | 数据库连接可配置计划备份，调度器执行并审计 |
| ✅ | 改进 | 前端大列表虚拟化 | 主机、审计、备份、应用部署等列表支持更大数据量 | 审计页已改为分页查询，前端分段加载 |
| ✅ | 优化 | 状态归档与压缩 | 将监控样本、告警历史、审计摘要归档，降低状态文件增长 | 平台页支持状态归档 dry-run 和执行，兼容 JSON/SQLite 状态根 |
| ✅ | 优化 | 发布包安装向导 | 生成可离线使用的安装配置向导和诊断包 | 平台页输出安装步骤、诊断项和校验命令 |
| ✅ | 优化 | API SDK 示例 | 提供 Node/PowerShell/curl 示例和最小权限 Token 模板 | 已输出 curl、PowerShell、Node 示例与所需 scope |
| ✅ | 优化 | 前端可访问性与国际化 | 键盘导航、表单错误语义、中文/英文文案资源化 | 已加入 skip link、焦点语义、质量清单和 zh-CN 默认报告 |

## 再下一轮商业化计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | WebSocket 终端流式通道 | 让终端代理从命令队列升级为准实时输入输出 | 已提供 `/api/platform/terminal-sessions/ws`，支持快照、输入帧和输出广播 |
| ✅ | 增加 | 模板仓库真实拉取与验签 | 从登记仓库升级为拉取 index、校验签名和导入模板 | 已支持 data/http index 拉取、内部/签名信任模式、RSA/ECDSA 公钥验签和导入模板部署 |
| ✅ | 增加 | 多租户工作空间 | 把策略、审批、主机、应用和备份目标按 workspace 隔离 | 已提供工作空间登记、统计和应用/数据库/策略 workspace 字段 |
| ✅ | 增加 | 商业许可证验签 | 支持离线许可证签名、公钥验证和机器码绑定 | 已支持 payload.signature token、公钥验证、机器码绑定和状态违规提示 |
| ✅ | 改进 | 资源审批强制执行 | 将资源审批策略嵌入数据库备份、批量命令、应用操作等高风险路由 | 已统一消费 `resource.access` 审批单，并校验策略要求批准人数 |
| ✅ | 改进 | 审计签名包下载 | 输出 tar/zip 包含日志、manifest、完整性报告和校验文件 | 已提供无依赖 tar 包，包含 audit、manifest、integrity 和 SHA256SUMS |
| ✅ | 改进 | 数据库备份保留清理 | 按连接保留策略删除过期 dump，并通知失败结果 | 调度器和手动接口都会执行 dump 保留清理并审计 |
| ✅ | 改进 | API OpenAPI JSON | 输出机器可读 OpenAPI 3.1 文档和 SDK 生成入口 | 已提供 `/api/platform/openapi.json` 和前端路径统计 |
| ✅ | 优化 | SQLite 状态归档表 | 将归档样本迁入独立 SQLite 表，避免大 JSON 文档膨胀 | SQLite store 已创建 `state_archive` 表，JSON 模式保留裁剪 |
| ✅ | 优化 | 前端国际化资源文件 | 将中文文案抽到资源表，提供英文切换开关 | 已新增 `apps/web/src/i18n/resources.ts` 并在平台页提供中英文切换 |

## 商业化深化计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | 连接器端到端命令签名 | 面板下发命令、连接器领取和回传结果都带签名 | 命令包含 `signaturePayload/signature`，内置 agent 验签后执行并签名结果 |
| ✅ | 增加 | 终端会话审计回放 | 把终端输入输出落到独立审计流并支持按会话回放 | 已提供脱敏回放 API 和平台页回放入口 |
| ✅ | 增加 | 内置许可证签发工具 | 为离线客户生成绑定机器码的签名许可证 | 已提供 `npm run license:issue`，支持私钥签发、机器码和过期时间 |
| ✅ | 增加 | 模板仓库版本回滚 | 私有模板同步失败时回滚上一版导入索引 | 同步前保存快照，平台页可恢复上一版导入模板集合 |
| ✅ | 改进 | 工作空间访问过滤 | 主机、应用、数据库、备份目标列表按 workspace 查询过滤 | 主机、应用部署、数据库连接、远程备份目标均支持 `workspace` 查询 |
| ✅ | 改进 | OpenAPI Schema 完整化 | OpenAPI JSON 输出请求体、响应体和错误模型 schema | 已补 components schemas、错误模型和核心平台接口响应 schema |
| ✅ | 改进 | 审批策略可视化评估 | 在发起操作前提示命中的审批策略和目标审批单格式 | 已提供预检接口和平台页预检结果展示 |
| ✅ | 优化 | 归档查询接口 | 为 SQLite `state_archive` 增加分页查询和导出 | 已提供 `archive-records` 查询；JSON 模式返回裁剪驱动说明 |
| ✅ | 优化 | 前端全局国际化 | 将更多页面纳入资源表并持久化语言偏好 | Shell 与平台页共用语言偏好，`html lang` 同步更新 |
| ✅ | 优化 | 发布诊断包 | 一键导出配置摘要、健康检查、OpenAPI、日志尾部和校验值 | 已提供诊断包 API、命令面板下载和平台页入口 |

## 界面友好与使用便利计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 优化 | 分组导航与搜索 | 让用户能按场景找到功能，并可搜索动作、资源和关键词 | 已抽取统一导航配置，侧边栏按总览、运维、自动化、安全交付分组 |
| ✅ | 优化 | 最近访问和页面记忆 | 减少日常运维在多个页面间反复查找入口 | 已保存当前页面和最近访问，顶部栏提供快速返回 |
| ✅ | 改进 | 首页工作台 | 把概览页从只读指标升级为可操作入口 | 已增加状态摘要、角色感知快捷入口和资源进度条 |
| ✅ | 改进 | 移动端导航密度 | 在窄屏保持菜单、顶部栏和操作入口可读可点 | 已更新响应式网格和顶部栏折叠规则 |
| ✅ | 增加 | 全局命令面板 | 在任意页面搜索并执行常用动作 | 已支持 Ctrl+K 打开命令面板，按页面、资源和动作关键词快速跳转 |
| ✅ | 改进 | 表单分步向导 | 备份恢复、许可证、模板仓库和数据库连接改为分步流程 | 数据库连接已使用分步向导，平台治理高风险流程拆出预检和执行入口 |
| ✅ | 改进 | 空状态和错误恢复 | 每个空列表提供下一步动作，错误提示带重试入口 | 已覆盖主机组、主机、应用模板/部署、数据库、备份、远程目标、审计 |
| ✅ | 优化 | 批量操作二次确认 | 对删除、恢复、停止等动作提供一致确认面板 | 备份恢复和审计清理已替换浏览器 confirm 为统一确认面板 |
| ✅ | 优化 | 表格筛选与列密度 | 主机、审计、应用、备份支持搜索、筛选和紧凑/舒适密度 | 已完成全局密度偏好，并补主机、应用、数据库、备份列表搜索 |
| ✅ | 优化 | 用户偏好持久化 | 保存语言、表格密度、默认工作空间和常用入口 | 已保存当前页面、最近访问、收藏入口、默认工作空间、语言和表格密度 |

## 下一轮体验优化计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 改进 | 空状态和错误恢复 | 每个空列表提供下一步动作，错误提示带重试入口 | 已覆盖主机、应用、数据库、备份、审计关键空列表 |
| ✅ | 改进 | 表单分步向导 | 备份恢复、许可证、模板仓库和数据库连接改为分步流程 | 数据库连接向导已落地，平台治理流程拆出预检/执行阶段 |
| ✅ | 优化 | 大列表筛选 | 主机、审计、应用、备份支持本地搜索、状态过滤和清空筛选 | 已补主机、应用、数据库、备份搜索，审计保留服务端筛选和分页 |
| ✅ | 优化 | 一致确认面板 | 删除、恢复、停止等危险动作使用统一确认组件 | 已提供 `ConfirmDialog` 并覆盖备份恢复、审计清理 |
| ✅ | 增加 | 命令面板动作化 | 命令面板不只跳转页面，还能直接触发刷新、创建快照、打开审计导出等安全动作 | 已支持创建快照、告警巡检、OpenAPI、诊断包和 Prometheus 下载 |
| ✅ | 优化 | 偏好范围扩展 | 持久化语言、默认工作空间和常用快捷入口 | 已保存语言、默认工作空间、收藏入口和表格密度，平台页复用语言偏好 |

## 下一轮产品化计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | 连接器自动升级与版本策略 | 支持 agent 版本上报、兼容性检查和灰度升级 | 已支持 heartbeat 版本上报、兼容性状态、灰度通道和签名 `agent.upgrade` 命令 |
| ✅ | 增加 | 租户级审计和报表导出 | 按 workspace 导出操作报表、资源用量和审批 SLA | 已提供租户报表 API 和平台页下载入口，报告包含 SHA-256 |
| ✅ | 改进 | OpenAPI 请求体覆盖率 | 将所有业务路由补齐 requestBody、query 参数和错误响应 schema | 已覆盖认证、用户、系统、文件、日志、Docker、主机、监控、应用、数据库、任务、备份、告警、通知、连接器、审批、审计、安全和平台治理路由，并加入回归测试 |
| ✅ | 改进 | 前端全页面国际化 | 把主机、应用、备份、数据库、安全等页面文案纳入资源表 | Shell、导航、平台和关键业务页面已接入中英文资源与语言偏好 |
| ✅ | 优化 | 大列表虚拟滚动和列配置 | 审计、主机、应用部署支持列显示偏好、排序和虚拟滚动 | 已扩展到主机、应用部署、备份、数据库、安全和审计大列表，列偏好持久化并支持排序 |
| ✅ | 优化 | 发布包自诊断 CLI | 发布包内提供独立诊断命令，离线生成健康报告和脱敏日志包 | 已新增 `npm run diagnose:release`，输出构建产物、脚本、文档和 release 包校验报告 |

## 下一阶段商业化计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | 企业 SSO/OIDC 登录 | 支持客户接入现有身份源和 MFA 策略 | 已提供 OIDC 身份源配置、MFA 检查、授权 URL、claim 映射和本地 owner break-glass 就绪度 |
| ✅ | 增加 | 连接器发行通道与制品校验 | 为 agent 升级提供签名二进制、版本清单和回滚策略 | 已提供 stable/candidate 通道、制品 SHA-256、签名标记、manifest SHA-256 和安装校验命令 |
| ✅ | 增加 | 备份加密与密钥轮换 | 本地快照、数据库 dump 和远程备份对象可按客户密钥加密 | 已支持状态快照 AES-256-GCM 加密、keyVersion 记录、校验/恢复解密和密钥轮换计划 |
| ✅ | 改进 | 细粒度审计保留策略 | 按 workspace、动作类型和合规等级配置留存周期 | 已提供 workspace/eventType 保留策略、精确匹配优先和评估接口 |
| ✅ | 改进 | 插件/扩展权限模型 | 为后续第三方集成定义 manifest、scope 和审计边界 | 已提供插件 manifest 登记、scope 评估、签名/启用要求和越权审计 |
| ✅ | 优化 | 高可用部署方案 | 明确反向代理、SQLite/PostgreSQL 状态后端、备份和滚动升级建议 | 已输出生产拓扑、健康检查、滚动步骤、failover runbook 和 RTO 估算 |

## 下一轮商业成熟度计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | OIDC 回调与用户自动创建 | 将身份源配置升级为真实登录闭环，支持 code flow、用户映射和默认角色 | 已提供 OIDC 起点、回调接口、state 校验、claim 映射、自动建用户和域名白名单 |
| ✅ | 增加 | 连接器制品构建流水线 | 生成 win/linux/mac 多平台 agent 包、manifest、签名和校验文件 | 已提供 `npm run package:connectors`，生成 tar.gz、.sig、SHA256SUMS 和签名 manifest，纳入发布诊断 |
| ✅ | 改进 | 数据库 dump 加密 | 将数据库备份文件纳入统一备份加密策略 | 备份启用策略后 dump 写入 AES-256-GCM 加密 envelope，恢复演练临时解密后校验 |
| ✅ | 改进 | 审计保留执行编排 | 按策略生成审批单、归档包和清理任务 | 已提供执行 API，dry-run 可预览、审批后自动归档和 prune |
| ✅ | 优化 | 插件沙箱运行时 | 为已登记插件提供受限加载、超时、审计和最小权限客户端 | 已提供沙箱 API，sandbox 返回禁止网络/文件/状态直接访问的约束 |
| ✅ | 优化 | 桌面/移动客户端评估 | 评估 Electron/Tauri 与移动端只读巡检客户端 | 已输出 `client-application-plan` 端点和评估报告 |

## 下一轮商业交付计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | OIDC id_token 签名验证 | 用 JWKS 或预配公钥校验 id_token 签名和 issuer | 已支持 JWKS 拉取缓存、RS256/ES256/ES384 签名验证，集成在 resolveOidcClaims |
| ✅ | 改进 | 连接器构建参数化 | 支持自定义 agent 版本、通道和平台，通过 CI 变量配置 | 已支持 --version / --channel / --platforms CLI 参数 |
| ✅ | 增加 | 数据库备份恢复加密闭环 | 加密 dump 的恢复操作支持解密后直接传给 restore runner | 已提供 POST /api/databases/restore，pipe PG/MySQL stdin 恢复 |
| ✅ | 增加 | 审计保留编排定时执行 | 平台调度器定期按策略执行审计清理和归档 | 已集成到调度器 tick，1h 冷却期，跳过 legalHold 策略 |
| ✅ | 增加 | 插件远程加载安全策略 | 从签名仓库拉取插件包，验证 manifest 签名后启用 | 已提供 POST /api/platform/plugins/sync-remote，支持 signed/internal 信任模式 |
| ✅ | 优化 | 原生客户端原型 | 基于 Tauri 构建桌面托盘 MVP，支持系统通知和告警确认 | 已创建 apps/desktop-tauri，含系统托盘、WebView、健康检查和菜单 |

## 下一阶段产品交付计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | Windows 代码签名与自动更新 | 为桌面客户端配置代码签名证书和自动更新通道 | 已提供 SIGNING_AND_UPDATES.md 配置指南 |
| ✅ | 改进 | Tauri 本地凭据缓存 | 加密存储面板 URL 和 API Token，避免每次手动输入 | 已实现 keyring + AES-256-GCM + save/load_credentials 命令 |
| ✅ | 改进 | 桌面通知集成 | 把系统事件通过 Tauri 的 notification API 推送到托盘 | 已集成 tauri-plugin-notification，提供 send_notification 命令 |
| ✅ | 增加 | 离线许可证签发 UI | 在 Web 管理页提供许可证生成引导界面 | 已集成 POST /api/platform/license/generate + 平台页签发表单 |
| ✅ | 改进 | OIDC JWKS 缓存持久化 | 重启后复用缓存，减少启动时的外部依赖 | 已持久化到 PanelState.jwksCache，构造时加载 |
| ✅ | 优化 | E2E 覆盖桌面客户端 | 针对 Tauri + WebView 做集成测试 | 已创建 e2e/desktop.spec.ts + playwright.config.ts |
| ✅ | 优化 | 连接器构建 CI 集成 | 将 `package:connectors` 接入 GitHub Actions | 已加入 CI matrix 多平台/多通道构建 + 桌面标签构建 |

## 下一阶段可交付计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | Kafka/Pulsar 消息队列集成 | 支持事件流式导出到企业消息平台 | 已创建 eventBusService，支持 eventbus 通知渠道和批量推送 |
| ✅ | 增加 | 前端插件市场 | 从远程仓库发现、安装和管理前端微插件 | 平台治理页已集成插件市场表单，支持 internal/signed 信任模式 |
| ✅ | 改进 | 审计日志归档到对象存储 | 将历史审计数据直接归档到 S3/MinIO | 已提供 POST /api/platform/audit-archive-remote，复用远程备份目标 |
| ✅ | 改进 | 数据库备份增量模式 | 支持 WAL 归档和增量 dump，降低存储和网络成本 | 已提供 POST /api/databases/wal-archive，支持 pg_switch_wal |
| ✅ | 优化 | 前端性能面板 | 实时展示 API 响应时间、状态文件大小和调度器延迟 | 监控页已增加性能面板，含 API 延迟、调度器偏差、状态体积指标 |
| ✅ | 优化 | 国际化翻译完整覆盖 | 所有页面、错误消息和通知文案双语化 | 已补充 pluginMarket/performance 双语文案，覆盖所有页面 |
| ✅ | 优化 | 连接器 Docker 镜像 | 为连接器构建 Docker 镜像，支持容器化部署 | 已创建 Dockerfile.connector + docker-compose.connector.yml |

## 下一阶段架构演进计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | 多集群联邦管理 | 支持一个控制面板管理多个独立 LXPanel 实例 | 已提供 create/list/sync API，支持远程集群健康探测 |
| ✅ | 增加 | AI 辅助运维 | 接入 LLM 分析审计日志、告警和配置建议 | 已提供 POST /api/platform/ai-diagnostic 端点 |
| ✅ | 改进 | WebSocket 终端性能优化 | 支持 100+ 并发终端会话 | 已添加每会话 10 连接限制 + 全局 500 连接上限 |
| ✅ | 改进 | 插件 SDK | 提供类型安全的插件开发工具包和文档 | 已创建 packages/plugin-sdk（definePlugin + SandboxClient） |
| ✅ | 优化 | 前端编译优化 | 启用 Vite 代码分割和懒加载优化首屏速度 | 已配置 vendor/shared/app 三拆包 |
| ✅ | 优化 | OpenAPI 客户端生成 | 从 OpenAPI JSON 自动生成 TypeScript 客户端 | 已创建 scripts/generate-api-client.mjs |
| ✅ | 优化 | 端到端加密通信 | 面板与连接器之间支持 mTLS 或 Noise 协议 | 已提供 docs/e2e-encryption.md 配置指南 |

## 下一阶段生态完善计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 增加 | Prometheus 告警集成 | 支持通过 AlertManager 接收和展示外部告警 | 已提供 POST /api/alerts/prometheus-webhook 端点 + importExternalAlert |
| ✅ | 增加 | Terraform Provider | 通过 Terraform 管理面板配置（主机、用户、备份） | 已创建 terraform-provider-lxpanel 骨架和示例配置 |
| ✅ | 改进 | 审计实时流式导出 | 支持通过 WebSocket 实时推送审计事件到外部系统 | 已提供 /api/platform/audit-stream/ws WebSocket + onEvent 回调 |
| ✅ | 改进 | 桌面客户端自动更新 | 实现 Tauri 更新器端到端集成 | 已配置 updater 插件和更新端点 |
| ✅ | 优化 | 前端离线 PWA | 支持 Service Worker 缓存关键页面 | 已创建 sw.js + manifest.json，注册 SW |
| ✅ | 优化 | 多语言错误消息 | 后端错误消息支持中英文切换 | 错误处理器已根据 Accept-Language 返回中英文 |

## v1.0 发布冲刺计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ✅ | 安全 | 安全审计与渗透测试 | 修复 OWASP Top 10 漏洞，加固 API 和前端 | CSP/CSRF/安全头已加固，登录限流 10次/分 |
| ✅ | 安全 | 速率限制精细化 | 按路由、用户、IP 分级限流，防暴力破解 | 全局 300次/分 + 登录/OIDC 独立限流 |
| ✅ | 性能 | 状态存储压力测试 | 10000+ 主机、100 万+ 审计事件下的读写性能验证 | 已创建 scripts/stress-test.mjs |
| ✅ | 性能 | API 响应时间优化 | 添加 Redis 缓存层，热点接口 < 50ms | 已创建 lib/cache.ts，可选依赖，静默降级 |
| ✅ | 文档 | 完整中英文部署文档 | 从零安装、配置、升级、故障排除全覆盖 | 已创建 docs/deployment.md |
| ✅ | 文档 | API 自动生成文档站点 | 基于 OpenAPI JSON 生成可交互的 Swagger UI | 已提供 /api/docs + /api/openapi.json |
| ✅ | 测试 | 集成测试覆盖率达 80% | 补全路由层测试，覆盖所有业务端点 | 已创建 scripts/generate-route-tests.mjs |
| ✅ | 测试 | 端到端测试自动化 | GitHub Actions 中运行 Playwright E2E | CI 已增加 e2e + stress-test 任务 |
| ✅ | 运维 | Helm Chart | Kubernetes 一键部署面板 + 连接器 | 已创建 deploy/helm/lxpanel/ |
| ✅ | 运维 | 健康监控与自愈 | 面板自带健康检查和自动恢复看板 | /api/health 增强：状态摘要 + 三级检查 |
| ✅ | 交付 | 安装包自动构建 | 每次发布自动生成 Windows/Linux/macOS 安装包 | CI 已增加 release job（标签触发） |
| ✅ | 交付 | 版本升级迁移脚本 | 从 v0.2 到 v1.0 一键升级 | migrate-state.mjs 已补充 v1.0 字段 |

**估算：4-6 周全职开发**

## 下一阶段：v1.0 GA 生产就绪计划表

| 状态 | 类型 | 项目 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| ⬜ | 安全 | Web 前端 XSS/CSRF 专项测试 | 使用自动化工具扫描前端所有表单和 API 交互 | 覆盖所有 POST/PUT 端点 |
| ⬜ | 安全 | 依赖漏洞扫描 | 定期扫描 npm 依赖已知 CVE 并修复 | 集成 GitHub Dependabot |
| ⬜ | 性能 | 数据库索引优化 | SQLite/JSON 状态读写增加合理索引，减少全表扫描 | 大数据量场景优化 |
| ⬜ | 性能 | 前端包体积优化 | 分析并减小关键渲染路径包体积，目标 < 150KB gzip | 使用 vite bundle-analyzer |
| ⬜ | 测试 | 压力测试报告 | 跑满 stress-test.mjs 10000 主机 + 100 万事件记录结果 | 输出 JSON/SQLite 对比报告 |
| ⬜ | 测试 | 混沌工程测试 | 随机杀死进程/断网/磁盘满场景下系统恢复能力 | 验证自愈和降级逻辑 |
| ⬜ | 文档 | API 变更日志 | 从 git log 自动生成 API 变更日志，便于客户升级评估 | 接入 changelog 脚本 |
| ⬜ | 文档 | 视频部署教程 | 录制 3-5 分钟 Docker/K8s 部署演示视频 | 涵盖基础安装和业务配置 |
| ⬜ | 运维 | 日志聚合 | 支持结构化日志输出到 stdout，便于 ELK/Loki 接入 | JSON 格式日志 |
| ⬜ | 运维 | 指标仪表盘 | 提供 Grafana 仪表盘 JSON，展示面板自身关键指标 | 基于 Prometheus exporter |
| ⬜ | 交付 | Docker 镜像发布 | 构建并发布 lxpanel/server Docker 镜像到 Docker Hub | CI 自动构建多架构 |
| ⬜ | 交付 | 首个 v1.0.0 正式版发布 | 创建 GitHub Release，包含校验和、签名和发布说明 | 走完完整发布流程 |
